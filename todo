change instancer to support ogl 3.1

for anything that takes workspace allocator, put that as first arg, unless after a normal allocator

defaults/mod.zig
layout should be maru.defaults.flat, not maru.flat

reorganize content/shaders

scenegraph
